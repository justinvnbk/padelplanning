spring.application.name=padelplanning

spring.datasource.url=jdbc:h2:mem:padelplanning
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

spring.web.locale=nl-NL

security.h2-console-needed=true

spring.mvc.format.date-time=yyyy-MM-dd'T'HH:mm

#Properties for mailing sandbox mailtrap
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=dacefe2cd6ad69
spring.mail.password=21b148d2b747b7
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Stripe configuration
stripe.public.key=${STRIPE_PUBLIC_KEY}
stripe.secret.key=${STRIPE_SECRET_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET}